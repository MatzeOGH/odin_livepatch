# Build artifacts
*.exe
*.pdb
*.obj

# The patch output directory patch() writes next to the exe
examples/livepatch/
# The patch DLLs, PDBs and maps that patch() writes next to the exe
livepatch_mod/
*.map
*.rdi
